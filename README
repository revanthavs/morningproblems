"""
##########################################################################
    Name: Avs Revanth
    Student_id: 1684293
    CMPUT 274 Fall 2020
    Weekly Exercise = 2
    Exercise: Unfair Dice and Histogram
##########################################################################
"""

Included Files:-
	README
	unfairDice.py

Notes and Assumptions:-
	
	Imported Random module
	form Random module the function used are:- 
	random.random to generate a random number form [0,1)
	random.seed to initialize seed value to use PRNG algorithm

	For biased_rolls() function:

	Arguments:- 
	prob_list : list of probabilites of rolling number on each side 
	s: seed value to initializing the PRNG
	n: the number of rolls

	Return:-
	rolls: a list containing each of the n rolls of the biased die in order they were generated

	Used methods:-
	len()
	append()

	Used lists:-
	rolls
	number_list
	prob_list
	prob_total

	In the while loop if the number found then break is used to break form the loop 

	For draw_histogram() function:

	Arguments:-
	m : The number of m-sided die
	rolls : The list of rolls generated by biased die
	width : The fixed width of the histogram

	Return:-
	There is no return value for this fucntion call

	Used methods:-
	len()
	append()
	round()
	max()

	Used lists:-
	count
	rolls

	In this function there are two blocks 
	Block-1 :- if number of rolls are greater than width
	Block-2 :- if number of rolls are less then width